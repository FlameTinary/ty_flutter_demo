import 'package:flutter/material.dart';

class XHSVideosPage extends StatelessWidget {
  const XHSVideosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('视频')),
      body: Center(
        child: Text('视频'),
      ),
    );
  }
}